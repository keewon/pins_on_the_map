# 특정 지점 기준 출퇴근 시간 표시

특정 지점을 중심으로 출퇴근 시간이 얼마나 걸리는지 등고선처럼 표시하는 서비스. 등고선처럼 연속적일 필요는 없고, 1km 단위면 충분할 것 같아.

- 특정 지점의 목록 (예. 역삼역, 판교역, 을지로입구역, 여의도역, 가산디지털단지, 제2판교테크노밸리, 잠실역, 성수역)
- Celery 를 이용하여 6시부터 10시까지 30분 단위로 시간을 체크. 지금 체크 기능도 있음.
- 교통수단은 대중교통, 자차, 자전거
- 카카오 API 가 있는지 확인할 것
- 출근 시간에는 특정 지점까지 오는 데 걸리는 시간 측정. 퇴근 시간에는 특정 지점부터 가는 데 걸리는 시간 측정
- 데이터를 지도에 표시 : 웹 앱(데스크탑, 모바일 둘 다 지원)
- Python 으로 static data 를 생성하고, 데이터를 HTML/JavaScript/CSS 로 표시
